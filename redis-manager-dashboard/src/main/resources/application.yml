server:
  tomcat.uri-encoding: UTF-8
  port: 8182

spring:
  application:
    name: redis-manager
  http:
    encoding:
      enabled: true
      force: true
      charset: UTF-8
#  datasource:
#    driver-class-name: com.mysql.cj.jdbc.Driver
#    url: jdbc:mysql://localhost:3306/redis_manager?useUnicode=true&characterEncoding=utf-8
#    username: xxx
#    password: xxx

# http://download.redis.io/releases/
# redis manager configuration
redis-manager:
  # installation configuration
  install:
    # physics machine
    machine:
      package-path: /redis/machine/
    # docker
    docker:
      package-path: /redis/docker/
